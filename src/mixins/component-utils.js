'use strict;'

export const variant = {
  props: {
    variant: {
      type: [Array, String],
      default: ''
    }
  }
}

export const autocompleteProp = {
  props: {
    autocomplete: {
      type: String,
      default: 'on'
    }
  }
}
